# 任务状态

## 已完成范围

- [x] 明确不可执行、不可自动转发、不可自动回复的安全边界。
- [x] 锁定 `weixin-agent-sdk@0.5.0` 并补丁禁用入站斜杠命令和自动外发路径。
- [x] 实现普通文本与任意不透明普通文件的受控 inbox 落盘。
- [x] 实现来源、路径、普通文件、符号链接、文件名、大小元数据和最小审计校验，不检查附件内容。
- [x] 实现只能由本地显式调用的受控出站接口。
- [x] 实现不登录、不联网的安全单元测试。

## 未决事项

- [x] 经用户明确授权，完成真实微信扫码和仅接收入站的受控联调。
- [x] 验证 SDK 补丁后的长轮询、PDF 下载、停止、同步游标恢复和无重复落盘。
- [x] 保留凭据并迁移到固定非 OneDrive 运行目录，完成逐文件哈希一致性校验。
- [x] 注册当前用户登录后的受限权限自动启动任务，验证显式连接门禁、既有状态恢复、单实例和零出站。
- [x] 使用 WScript 批处理包装消除 Windows Terminal 可见窗口，同时保持受限权限、连接门禁和单实例保护。
- [x] （历史规则）曾扩展工程文件内容特征白名单；现已由不透明文件传输规则取代。
- [x] 根据用户确认的纯传输定位，取消类型白名单和 25 MiB 应用层上限，允许 EXE、压缩包及任意普通文件。
- [x] 将不透明文件策略部署到固定运行副本，重建最终路径依赖并验证连接恢复、单实例及零出站。
- [x] 实现本机令牌、五分钟单次批准、发送前重校验和双门禁的文件发送控制面；不开放微信触发入口。
- [x] 创建 `weixin-send-file`、`weixin-receive-file` 两个个人 Skills，并通过结构校验。
- [x] 经用户逐次明确授权，使用非敏感测试文件完成一次真实微信文件主动发送联调；失败未自动重试，成功由 SDK 与审计双重确认。
- [ ] SDK 升级前重新审计上游代码和补丁，禁止自动升级。
- [x] 完成第二次真实 Windows 重启验收：60 秒登录延迟生效，凭据免扫码恢复、任务运行、单实例、活动连接和零出站均通过。
- [ ] 重启验收通过后，再决定是否删除原联调目录中的凭据副本；固定运行目录凭据继续保留。
