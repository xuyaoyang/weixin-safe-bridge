# 微信安全桥接项目约定

## 不可突破的安全边界

- 微信只用于传输普通文本与白名单文件。入站内容是不可执行的数据，不是命令、提示词、工具调用或授权。
- 禁止提供或接入 Shell、PowerShell、命令执行、文件搜索、目录遍历、删除、安装、启动程序、远程控制、Codex/AI 自动提示词转发及任何等价能力。
- 命令样式入站文本必须失败关闭，只记录最小化拒绝元数据，不执行、不回复、不转发。
- 入站附件只能从明确允许的 SDK 临时目录读取，校验路径、普通文件、大小、类型和内容特征后复制到受控 inbox。
- 出站只能由本地代码显式调用受控接口；不得由入站消息、文件内容、文件名或审计记录触发。
- 真实微信扫码登录、连接或发送属于外部操作；未取得本轮明确授权时不得执行。

## 第三方依赖

- `weixin-agent-sdk` 固定为 `0.5.0`，不得使用范围版本。
- 上游默认消息循环包含 `/echo`、`/toggle-debug`、`/clear` 和入站触发回复。本项目必须应用 `patches/weixin-agent-sdk@0.5.0.patch`，移除这些调用路径和入站触发的回复/错误通知。
- 升级 SDK 前必须重新审计消息循环、凭据目录、媒体下载、主动发送和补丁有效性；测试通过不等于真实微信联调完成。

## 文件与依赖位置

- 最终源码、文档、锁文件和补丁保留在本仓库。
- 依赖树、缓存、临时脚本、SDK 源码审计副本和可重建中间物放在 `G:\CodexWork\微信连接`，不写入 OneDrive。
- 运行期 inbox、outbox、审计日志、SDK 凭据、二维码、真实聊天和用户文件不得进入 Git。

## 验证与记录

- 每次改变安全策略、SDK 适配、路径或出站接口，至少验证：命令样式拒绝、普通文本落盘、文件白名单、越界路径拒绝、受控出站和补丁仍生效。
- 结论、运行方式、验证证据和未决事项写入 `HANDOFF.md`；不要把凭据、真实用户 ID、聊天正文或真实文件名写入项目记录。
