# 安全边界与威胁模型

## 信任边界

微信发送者、入站文本、附件内容、附件文件名、MIME 声明和 SDK 临时路径均不可信。本地运维人员显式调用受控 API 才是出站授权来源；“收到一条微信消息”本身绝不构成授权。

## 入站控制

1. 来源标识必须非空、长度不超过 256，且不含控制字符；落库和审计只保存 SHA-256 摘要。
2. 文本按 UTF-8 字节数限制为 16 KiB，拒绝 NUL 和非换行/制表控制字符。
3. 以 `/`、`!`、`>`、`$`、`&`、Shell 名称、Windows 可执行路径或脚本相对路径开头的命令样式文本失败关闭。
4. 附件只允许来自配置的目录；拒绝目录、符号链接、重解析越界、超 25 MiB 文件和不支持的类型。
5. 文件类型同时检查扩展名、MIME 声明和内容特征。通用 ZIP、脚本、可执行文件、快捷方式、安装包和动态库始终拒绝。
6. 接受记录写入独立消息目录；正文、附件和元数据均使用排他创建，避免静默覆盖。

## 出站控制

1. 出站文本/文件只能由本地代码调用 `createOutboundController()`。
2. `send()` 只接受同一控制器 `prepare()` 产生的对象；普通对象或其他控制器对象被拒绝。
3. 出站文件必须位于受控 `outbox`，并执行与入站相同的文件校验；不允许 URL、越界路径或符号链接。
4. 入站 Agent 永远返回空对象。SDK 补丁移除斜杠命令、自动回复、自动错误通知和打字状态发送。
5. 每次出站尝试、成功或失败写入审计日志；不记录正文。

## 依赖与凭据

- 锁定 `weixin-agent-sdk@0.5.0` 与锁文件完整性；补丁是安全边界的一部分。
- SDK 的状态目录通过包装器定向到项目数据目录下的 `sdk-state`。二维码、令牌、同步游标和账号数据不得进入 Git。
- 真实登录/连接需要环境开关和命令行确认双门禁；本轮未执行真实联调。

## 报告安全问题

请通过 GitHub 仓库的私密安全报告渠道提交。不要在公开 Issue 中粘贴令牌、二维码、真实聊天或用户文件。
