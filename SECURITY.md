# 安全边界与威胁模型

## 信任边界

微信发送者、入站文本、附件内容、附件文件名、MIME 声明和 SDK 临时路径均不可信。本地运维人员显式调用受控 API 才是出站授权来源；“收到一条微信消息”本身绝不构成授权。

## 入站控制

1. 来源标识必须非空、长度不超过 256，且不含控制字符；落库和审计只保存 SHA-256 摘要。
2. 文本按 UTF-8 字节数限制为 16 KiB，拒绝 NUL 和非换行/制表控制字符。
3. 以 `/`、`!`、`>`、`$`、`&`、Shell 名称、Windows 可执行路径或脚本相对路径开头的命令样式文本失败关闭。
4. 附件只允许来自配置的 SDK 临时目录；拒绝目录、符号链接和重解析越界。普通文件不设 25 MiB 应用层上限。
5. 文件按不透明载荷复制，不读取内容做类型、病毒或哈希判断。EXE、脚本、压缩包、宏文档和未知扩展名均可保存，但入站流程不得打开、解压、解析或执行它们。
6. 接受记录写入独立消息目录；正文、附件和元数据均使用排他创建，避免静默覆盖。

## 出站控制

1. 出站文本/文件只能由本地代码调用 `createOutboundController()`。
2. `send()` 只接受同一控制器 `prepare()` 产生的对象；普通对象或其他控制器对象被拒绝。
3. 出站文件必须位于受控 `outbox`，只允许本地普通文件；不允许 URL、越界路径、目录或符号链接。
4. 本地控制接口使用运行目录内的随机令牌；令牌目录在 Windows 上限制为当前用户和 SYSTEM。本地准备返回五分钟有效、单次使用的批准 ID，准备阶段不调用传输。
5. 真实提交还要求客户端环境开关、明确确认参数和服务端确认字段；发送前重新核对文件系统身份、大小和时间状态，明显替换文件会失败关闭，但不读取内容或计算项目级文件哈希。
6. 入站 Agent 永远返回空对象。SDK 补丁移除斜杠命令、自动回复、自动错误通知和打字状态发送；入站模块不引用本地控制客户端。
7. 每次出站尝试、成功、失败或重校验拒绝写入审计日志；不记录正文。

## 依赖与凭据

- 锁定 `weixin-agent-sdk@0.5.0` 与锁文件完整性；补丁是安全边界的一部分。
- SDK 的状态目录通过包装器定向到项目数据目录下的 `sdk-state`。二维码、令牌、同步游标和账号数据不得进入 Git。
- 入站产生的 `context_token` 仅保存在 ACL 受限的 `sdk-state`，采用临时文件后原子替换，并按 23 小时保守有效期读取；本地状态接口不返回令牌、账号或用户标识。
- 真实登录/连接需要环境开关和命令行确认双门禁；真实文件主动发送还需要短时单次批准。只有 SDK 返回 `sent` 且审计出现 `OUTBOUND_SENT` 才能声称成功。

## 报告安全问题

请通过 GitHub 仓库的私密安全报告渠道提交。不要在公开 Issue 中粘贴令牌、二维码、真实聊天或用户文件。
