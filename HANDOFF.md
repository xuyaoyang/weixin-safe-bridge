# 微信安全桥接 HANDOFF

## 项目目标

在不依赖 OpenClaw 运行时的前提下，复用锁定版本的 `wong2/weixin-agent-sdk`，只提供微信普通文本与文件的安全收件箱，以及本地显式受控的出站接口。

## 当前状态

- 项目从空目录新建。
- 当前源码版本为 `0.3.1`，文件通道定位为“不透明普通文件传输”。
- 第三方依赖固定为 `weixin-agent-sdk@0.5.0`；Node.js 要求 `>=22`。
- 已审计上游默认消息循环：其内置 `/echo`、`/toggle-debug`、`/clear`，并含 Agent 返回值自动回复、异常通知和打字状态发送。项目补丁移除了这些入站触发路径。
- 入站业务代码只校验文本并把附件按不透明普通文件保存到受控 inbox；命令样式文本、越界路径、目录和符号链接失败关闭，附件内容、类型和哈希不检查。
- 出站由本地 `createOutboundController()` 的 `prepare()`/`send()` 两阶段接口控制；入站代码没有对该控制器的引用。
- 2026-08-17 已完成一次受控真实微信“仅入站”联调：扫码登录、长轮询、普通文本、PDF、命令样式拒绝、停止和无消息重启均已验证；未执行任何主动发送。
- 2026-08-17 已把 SDK 凭据和同步状态复制到固定运行目录 `G:\CodexWork\微信连接\service\runtime-data\sdk-state`，逐文件相对路径、长度和 SHA-256 汇总校验一致；原联调目录暂未删除。
- 当前用户登录后自动启动任务 `WeixinSafeBridge` 已注册并运行，使用受版本管理的 `scripts/start-windows-bridge.ps1`。任务为受限权限、隐藏窗口、单实例、无运行时长上限；启动脚本还会拒绝缺少显式真实连接确认参数的调用，并阻止遗留进程造成重复实例。
- 为兼容 Windows Terminal 默认控制台宿主，任务动作通过 `wscript.exe //B //Nologo` 运行受版本管理的 `scripts/start-windows-bridge.vbs`，再以窗口样式 0 启动原 PowerShell 脚本；任务仍使用当前用户受限权限，不请求提升权限。
- 文件策略已按用户确认的“纯文件传输”目标改为不透明模式：EXE、脚本、安装包、压缩包、宏文档、未知扩展名和超过 25 MiB 的普通文件均不被应用层类型/大小规则拒绝；程序不打开、解析、解压或执行附件。
- 本地文件收发通过 `src/file-transfer-cli.mjs` 和两个个人 Skills 固化：收件只列出最小附件收据并按精确引用无覆盖导出；发件使用受 ACL 保护的随机令牌、五分钟单次批准、客户端双门禁和发送前文件系统状态重校验。微信入站模块不能调用该控制面。
- 项目不再计算附件 SHA-256 或记录附件内容特征，版本锁定补丁同时移除 SDK 入站的 100 MiB 固定保存上限。SDK 为满足微信上传协议仍会整文件缓冲并计算 MD5；可用内存、磁盘及微信 CDN 仍可能限制超大文件，取消应用层上限不等于任意大小必然发送成功。
- SDK 补丁会把最新入站 `context_token` 原子写入受限 `sdk-state`，按 23 小时保守有效期跨进程恢复；本地认证状态接口只返回可用性和时间，不返回令牌、账号或用户标识。令牌真实过期或被服务端撤销后仍需一条普通入站消息刷新。

## 运行方式

1. 在非 OneDrive 工作区安装依赖并运行 `pnpm test`。
2. 设置 `WEIXIN_BRIDGE_DATA_DIR` 到非仓库目录。
3. 只有取得当次明确授权后，才可设置真实登录/连接开关并运行 README 中的双门禁命令。
4. 固定 Windows 部署采用 `<service-root>\app` 源码与同级 `runtime-data`、`logs` 目录；登录后启动任务必须由本地显式传入 `-ConfirmRealConnection`，微信入站内容无权创建、修改或触发该任务。

## 验证证据

- 单元测试覆盖命令样式拒绝、普通文本落盘、白名单 PDF 落盘、越界/脚本附件拒绝、不安全出站路径拒绝、同控制器准备对象约束和 SDK 补丁存在性。
- `scripts/verify-installed-sdk.mjs` 会直接检查安装后的 SDK 包，确认入站处理段不再调用斜杠命令、自动文本/文件发送、错误通知或打字状态。
- 2026-08-16 在 `G:\CodexWork\微信连接\install-check` 使用 bundled Node.js 24.19.0 与 pnpm 11.19.0 执行锁文件安装：安全测试 10/10 通过，安装后补丁检查通过，`pnpm audit --prod --audit-level high` 报告 0 个已知漏洞。
- CI 使用 Node.js 24、锁文件安装并执行测试。
- 2026-08-17 在独立目录 `G:\CodexWork\微信连接\real-inbound-20260817-094051` 联调前重新执行 frozen-lockfile 安装、10/10 安全测试、安装后 SDK 扫描和生产依赖审计，均通过。
- 真实入站结果为：普通文本接受 1 条、`application/pdf` 附件接受 1 条、命令样式文本以 `COMMAND_STYLE_TEXT` 拒绝 1 条；审计中 `OUTBOUND_*` 事件为 0，outbox 文件为 0。审计字段只有时间、事件、消息 ID、来源摘要、内容哈希、类型标志和拒绝码，不含聊天正文、完整来源或文件路径。
- 首次停止后桥接进程为 0；再次启动成功恢复既有同步游标，未重复落盘，随后再次停止并确认桥接进程为 0。
- 固定运行目录安装后再次通过 10/10 安全测试、安装后 SDK 扫描和生产依赖审计；凭据迁移前后 3 个非空状态文件的相对路径、长度和 SHA-256 汇总一致。
- 自动启动任务首次因旧版 Windows PowerShell 误读脚本中写死的中文用户目录而返回 1；改为运行时读取系统用户目录后复验通过。任务状态为运行中，桥接进程 1 个，既有同步状态恢复、监听启动、无“未登录账号”错误，outbox 文件为 0。
- 启动脚本无 `-ConfirmRealConnection` 时失败关闭；带确认参数重复启动时正常退出且桥接进程仍为 1 个，单实例保护通过。
- 2026-08-17 首次真实 Windows 重启验收未通过：任务在开机后约 10 秒触发，但以 `0xC000013A` 被系统中断，桥接进程为 0；SDK 状态 3 个文件仍完整，outbox 和 `OUTBOUND_*` 审计均为 0。当前会话内重新启动后无需扫码即可恢复监听，证明凭据有效，因此问题初步定位在登录初期启动时序，而非凭据失效。
- 登录触发增加 60 秒延迟后，第二次真实 Windows 重启验收通过：任务在开机约 70 秒后自动进入运行状态，桥接进程在本次开机后启动且仅有 1 个，SDK 状态 3 个文件完整，存在活动网络连接，无“未登录账号”错误，outbox 和 `OUTBOUND_*` 审计均为 0；重复触发任务后仍保持单实例。该结果证明当前凭据可免扫码自动恢复，但微信侧令牌未来失效或被撤销时仍可能要求重新扫码。
- 交互式 PowerShell 任务虽带 `-WindowStyle Hidden`，仍被 Windows Terminal 显示为空白窗口。系统拒绝在非提升权限下改为 S4U 后，改用 WScript 无窗口包装；重启任务后进程链为 `wscript.exe` → `powershell.exe` → `node.exe`，新增控制台宿主只有无可见主窗口的隐藏 `conhost.exe`，桥接进程 1 个、活动连接存在、outbox 为 0。
- 历史白名单版本曾验证真实 DWG 头、常用工程格式和内容匹配；该规则现已按用户要求被不透明传输策略取代。
- 不透明文件策略离线测试覆盖 EXE、ZIP、7Z、脚本、伪装扩展名、26 MiB 稀疏文件、越界路径、目录、可选调用方大小上限、批准不可重放、批准后文件替换拒绝和收件无覆盖导出。全套 Node 测试现为 20 项通过。
- `weixin-send-file` 与 `weixin-receive-file` 安装在本机 Codex Skills 目录并通过 `skill-creator` 的 `quick_validate.py`；发件 Skill 只有在本地用户当前请求明确给出唯一绝对路径和“发送”动作时才提交，预检请求不外发。
- 固定运行副本已快进到提交 `0b7ae29`，锁文件安装、19/19 测试、安装后 SDK 补丁扫描和生产依赖审计通过。任务重启后桥接进程 1 个且连接活动；`local-control` 和默认 `received` 目录均关闭 ACL 继承，只允许当前用户与 SYSTEM。本地令牌握手只执行了“不存在批准的取消”并成功返回，未创建批准；outbox 文件和 `OUTBOUND_*` 审计仍为 0。
- 2026-08-17 经用户逐次明确授权完成首次真实主动文件发送：前两次提交失败且未自动重试，微信产生新入站上下文后，一个非敏感 Markdown 文件由 SDK 返回 `sent`，审计顺序为 `OUTBOUND_ATTEMPT` 后 `OUTBOUND_SENT`。项目记录不保存真实文件名、聊天正文或用户标识。
- 2026-08-20 在 `G:\CodexWork\微信连接\context-token-check-20260820` 完成独立 frozen-lockfile 安装；23/23 测试、安装后 SDK 扫描和生产依赖审计通过。新增测试确认状态接口不泄露令牌/账号、缺失/损坏/未来时间/超过 23 小时均失败关闭，状态查询不准备或发送消息。
- 固定运行副本首次应用 `sdk-state` ACL 时，`icacls /T` 未给已关闭继承的现有凭据文件写入访问 ACE，导致 SDK 误报未登录；文件仍存在且未覆盖。已按目录与文件分别设置当前用户和 SYSTEM 的显式权限，并修正启动脚本，避免依赖继承状态。
- 2026-08-20 固定运行副本已更新到 `0.3.0`（功能提交 `2bc3916`、ACL 修复提交 `166e4d3`），旧副本备份在 `G:\CodexWork\微信连接\service\backups\app-before-2bc3916-20260820`，运行数据未移动。最终路径强制重建依赖后 23/23 测试和安装后 SDK 扫描通过；计划任务为 Running，桥接进程 1 个、活动连接 1 条、近期关键错误 0，本次启动后无新增出站事件。
- 部署后只读状态为 `available=false`：旧版只存在于内存的令牌无法从已停止进程迁移。需要用户发送一条普通微信消息完成首次令牌落盘；此后才能正式验收 23 小时窗口内跨进程恢复，当前不能把离线测试当作真实微信令牌恢复验收。
- 2026-08-20 用户两次表示已从微信发送普通消息，但固定桥接在重启前后均未出现新 inbox 记录或 `context-tokens.json`；重启后计划任务 Running、进程 1 个、活动连接 1 条且同步游标继续更新。当前只能确认长轮询在线，不能确认消息送达当前登录 Bot，也不能完成真实令牌持久化/恢复验收；需核对微信端实际对话是否为本机扫码登录的同一 Bot。
- 2026-08-20 按用户要求完成真实运行副本 A/B 回退：从升级前备份精确恢复 `0.2.0`，确认 20/20 旧版测试及旧 SDK 补丁哈希后启动；等待期间同步游标继续更新，但 inbox 仍停在 2026-08-18，用户此前发送的消息同样未收到。由此排除 `0.3.0` 令牌持久化补丁导致当前入站中断的假设。对比结束后已恢复 `0.3.0`，23/23 测试通过，任务 Running、进程 1 个、活动连接 1 条；下一步应重新扫码核对 Bot 会话绑定，而不是继续回退代码。
- 2026-08-20 经用户授权完成重新扫码登录，凭据索引与活动账号文件成功更新；用户明确纠正微信端只允许一个 Bot，新扫码会使旧绑定失效，因此“新账号文件 ID 等于第二个 Bot 对话”的推断已撤回。新绑定后 SDK monitor 仍收不到入站；停止 monitor 后直接请求当前 token 的 `getupdates`，使用现有游标和空游标均为 HTTP 200、`msgs=[]`，响应含不同的 `get_updates_buf`/兼容 `sync_buf`。诊断加入腾讯当前实现的 `iLink-App-Id`、`iLink-App-ClientVersion` 和当前元信息后结果仍为空，故暂不能把缺头认定为根因，也未把诊断改动部署到 SDK。npm `latest` 仍为 `weixin-agent-sdk@0.5.0`。桥接已恢复 Running；当前结论是微信服务端未向新 token 交付消息，真实令牌持久化验收仍未完成。
- 2026-08-20 对唯一 Bot 执行元数据最小化的在线状态诊断：`notifyStart` 返回 HTTP 200、`ret=0`，随后连续 3 分钟、10 次 `getupdates` 轮询未取得消息元数据；由于不能确认用户是否在该窗口内发送，该结果不作为消息交付失败证据。依据腾讯当前实现补入 `iLink-App-Id`、`iLink-App-ClientVersion`、启动 `notifyStart` 和正常停止 `notifyStop`，同时保留所有入站禁命令、禁自动外发边界；版本更新为 `0.3.1`。
- `0.3.1` 首次运行部署暴露了静态字符串扫描的缺口：零上下文 SDK 补丁把生命周期函数放入 `getUpdates` 局部作用域，启动日志明确报 `ReferenceError: notifyStart is not defined`。该副本未通过运行验收，已保留于 `service\backups\app-0.3.1-broken-notifystart-scope-20260820`；补丁随后改为替换函数闭合行，并新增顶层作用域结构检查。修正版在独立目录与最终运行路径均通过 23/23 测试和安装后 SDK 扫描，计划任务为 Running、桥接进程 1 个、活动连接 2 条，新启动日志不再出现 `notifyStart` 未定义、拒绝或网络错误。旧 `0.3.0` 副本保留于 `service\backups\app-0.3.0-before-0.3.1-20260820`，运行数据和凭据未移动；消息接收仍需用户向原来的唯一 Bot 发送普通文本完成真实验收。
- 2026-08-17 在 `G:\CodexWork\微信连接\opaque-transfer-check-20260817-2250` 完成独立 frozen-lockfile 安装，20/20 测试通过，生产依赖审计为 0 个已知漏洞，安装后 SDK 扫描确认 100 MiB 固定入站保存上限已移除，斜杠命令和入站自动外发路径仍被补丁移除；项目与本机两个 Skills 均通过 `quick_validate.py` 且内容一致。
- 用户明确授权后，固定运行副本已切换到提交 `314370b` 的 `0.2.0`。旧 `app` 保留于 `G:\CodexWork\微信连接\service\backups\app-before-314370b-20260817-2321`；`runtime-data`、凭据、inbox、outbox 和审计未移动。首次启动因旁路安装的 pnpm 链接仍指向原 `app-next` 路径而失败，随后在最终 `service\app` 路径执行 frozen-lockfile 强制重建依赖并再次通过 20/20 测试和 SDK 补丁扫描。计划任务最终为 `Running`，WScript/PowerShell/Node 各 1 个，恢复既有同步游标并建立活动连接；本次重启后的 `OUTBOUND_*` 事件为 0，未发送任何消息。

## 发布边界

- 公开仓库只能包含源码、测试、文档、锁文件和最小第三方补丁。
- 不得包含运行数据、真实聊天、附件、用户 ID、二维码、令牌、SDK 状态、日志、`node_modules` 或缓存。
- 公开仓库目标地址：`https://github.com/xuyaoyang/weixin-safe-bridge`。

## 未决事项

- 不透明文件策略已在固定运行副本生效；尚未使用 EXE、压缩包或超过 25 MiB 的真实用户文件做端到端微信传输，离线通过不等于微信 CDN 对任意大小均会接受。
- 当前保留固定运行目录和原联调目录两份 SDK 状态。第二次重启验收已经通过；是否清理原联调副本仍需用户明确确认，固定运行目录凭据继续保留。
- 微信客户端侧“确实没有出现回复”未由独立客户端自动化取证；本地安装后 SDK 扫描、出站审计和 outbox 均为零，但如需正式验收仍应补用户侧截图或双端观察记录。
